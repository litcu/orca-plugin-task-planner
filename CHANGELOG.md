# 更新日志

## v0.9.0 - 2026-05-11

### 新增

- 新增“项目”功能，支持项目标签、项目视图和项目属性面板，便于按项目组织任务。
- 支持在笔记中显示“我的一天”的排期时间，并扩展更多将任务添加到“我的一天”的入口。

### 优化

- 优化任务视图页签与列表交互。

### 修复

- 修复部分面板空状态下的 React hook 崩溃问题。
- 修复面板缩放时可能触发隐藏子树渲染的问题。

## v0.8.5 - 2026-05-05

### 新增

- 新增任务计时器运行时入口，在头部栏展示当前计时状态，并提供计时弹窗。
- 任务状态图标改为打开状态选择菜单，可直接选择任意任务状态。
- 任务列表、所有任务视图和“我的一天”任务卡片支持直接设置指定状态，并在右键菜单中新增“设置为已完成”。
- 任务标签菜单新增“设置为已完成”快捷操作。

### 优化

- “我的一天”仅保留日程模式，移除列表/日程切换入口，让规划视图更聚焦。
- 添加任务到“我的一天”时，今日日志中改为创建任务块引用，不再创建镜像块。
- 优化仪表盘和“我的一天”的深色主题适配。
- 优化任务视图刷新逻辑，复用任务数据快照与变更签名，减少不必要的全量刷新。
- 优化计时器刷新与持久化逻辑，只更新可见计时信息，并在页面隐藏、关闭或卸载时保存运行中的计时状态。

### 修复

- 修复应用重启后任务计时器恢复不正确的问题。
- 修复任务刷新与计时器入口在部分场景下不同步的问题。
- 修复手动插入任务标签时默认任务属性和任务元数据未初始化的问题。
- 修复“我的一天”旧占位文本、重复条目和旧日程分组残留导致的同步异常。
- 修复完成任务后计时器仍可能继续运行的问题。
- 修复主状态循环会直接进入完成状态的问题；现在状态循环只在待办和进行中之间切换，完成需通过状态菜单或“设置为已完成”操作明确触发。

